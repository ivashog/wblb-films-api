# Migration `20200904125453-fix-actors-to-films-relation-name`

This migration has been generated by Olexiy Ivashkin at 9/4/2020, 3:54:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."__actors_to_films" DROP CONSTRAINT "__actors_to_films_A_fkey"

ALTER TABLE "public"."__actors_to_films" DROP CONSTRAINT "__actors_to_films_B_fkey"

CREATE TABLE "public"."_actors_to_films" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE UNIQUE INDEX "_actors_to_films_AB_unique" ON "public"."_actors_to_films"("A", "B")

CREATE INDEX "_actors_to_films_B_index" ON "public"."_actors_to_films"("B")

ALTER TABLE "public"."_actors_to_films" ADD FOREIGN KEY ("A")REFERENCES "public"."actors"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_actors_to_films" ADD FOREIGN KEY ("B")REFERENCES "public"."films"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."__actors_to_films"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200904122300-initial-migration..20200904125453-fix-actors-to-films-relation-name
--- datamodel.dml
+++ datamodel.dml
@@ -3,18 +3,18 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Actor {
   id         Int      @id @default(autoincrement())
   fullName   String   @unique @map("full_name")
   filmsCount Int      @default(1) @map("films_count")
   createdAt  DateTime @default(now()) @map("created_at")
   updatedAt  DateTime @updatedAt @map("updated_at")
-  films      Film[]   @relation("_actors_to_films")
+  films      Film[]   @relation("actors_to_films")
   @@map("actors")
 }
@@ -24,9 +24,9 @@
   releaseYear Int         @map("release_year")
   formatId    Int?        @map("format_id")
   createdAt   DateTime    @default(now()) @map("created_at")
   format      FilmFormat? @relation(fields: [formatId], references: [id])
-  actors      Actor[]     @relation("_actors_to_films")
+  actors      Actor[]     @relation("actors_to_films")
   @@index([name])
   @@unique([name, releaseYear])
   @@map("films")
```


